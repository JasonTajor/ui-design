# <Component Name>

> Copy this file for every new component. Document intent and rules here; the
> real source file is the source of truth for the exact API.

## Source of truth
`path/to/Component.tsx` — read it before relying on this spec.

## Purpose
One sentence: what this is for and when to use it (vs. alternatives).

## Variants
List each variant and when to use it.

## Props (intent, not full signature)
Key props and what they mean. Defer exact types to the source.

## Tokens used
Spacing / color / radius / type / motion tokens this component consumes.
(Must be tokens only — never literals.)

## States
default · hover · active · focus-visible · disabled · loading · error (as applicable)

## Accessibility
Roles, keyboard interaction, focus handling, labels.

## Do / Don't
- Do: ...
- Don't: ... (especially: don't fork this to make a near-duplicate)
